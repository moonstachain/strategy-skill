interface:
  display_name: Strategy Skill Template
  short_description: Convert a confirmed strategy into a reusable Codex skill template.
  default_prompt: Use $strategy-skill-template to convert a confirmed strategy into
    a reusable Codex skill template.
